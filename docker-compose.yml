services:
  forty-two-watts:
    build: .
    image: forty-two-watts:latest
    container_name: forty-two-watts
    restart: unless-stopped
    network_mode: host  # needed for MQTT/Modbus access to local devices
    volumes:
      - ./data:/app/data
