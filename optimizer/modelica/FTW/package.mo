within ;
package FTW
  extends Modelica.Icons.Package;

  annotation (
    uses(Modelica(version = "4.1.0")),
    Documentation(info = "<html>
<p>Small physical reference models that keep the same site boundary and units as FTW.</p>
</html>"));
end FTW;
