---
"ftw": patch
---

Core now keeps a driver out of control when the driver registry has blocked writes pending a safe default, tracks refused battery, PV, and EV actions separately on hybrid devices, and prevents an excluded charger from sending again before its timed retry.
