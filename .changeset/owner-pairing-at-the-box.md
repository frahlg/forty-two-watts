---
"ftw": patch
---

An owner pairing code can only be minted on the box itself, or after the house password is on. The same gate covers promoting a phone to owner and the first pairing on an empty box, which would otherwise become an owner. A viewer invite still works from the LAN once an owner exists.
