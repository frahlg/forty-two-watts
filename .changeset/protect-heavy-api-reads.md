---
"ftw": patch
---

Require the same local-or-token check for config, logs, support dumps, device identity, integration status, notification history and local paths that already guards backups and CalDAV passwords. Live status, energy, prices and plan reads stay open.
