---
"ftw": patch
---

Require the same local-or-token check for config, logs, support dumps, driver source and system info that already guards backups and CalDAV passwords. Live status and energy reads stay open.
