---
"ftw": patch
---

Reject invalid MPC physics before either solver and keep the prior plan. Use one PV uncertainty snapshot per replan, and do not use the Go fallback while a battery is recovering into its operating SoC range.
