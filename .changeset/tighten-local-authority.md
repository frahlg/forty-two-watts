---
"ftw": patch
---

A hostname with no dot is no longer treated as local. If the driver catalog cannot be read, config secrets stay hidden. Driver test and fingerprint refuse loopback and link-local targets.
