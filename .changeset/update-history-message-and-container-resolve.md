---
"ftw": patch
---

Update Center history now shows the failure message, not only "failed". The updater also ignores non-ID noise when resolving the optimizer/core container, which blocked some stable→beta Core updates after a successful snapshot.
